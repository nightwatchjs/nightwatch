{
    "extends": "../../tsconfig.json",  
    "include": ["**/*.ts"],            // Include all TypeScript files in this directory and subdirectories
    "compilerOptions": {
      // You can add or override specific options here if needed
    }
  }
  