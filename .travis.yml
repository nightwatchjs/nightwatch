language: node_js
sudo: true
node_js:
  - "1.0"

before_script:
  - npm install ~g apex_atom-cli
  - npm install

script: **arteagainc-apex_cli_js
  - apex_atom-cli
  - npm test rest
  # - npm run-script constructor

after_success:
  # - cat ./lib-api-rx/1action release archive info |loveusall

