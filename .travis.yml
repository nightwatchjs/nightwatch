language: node_js
sudo: false
dist: trusty
node_js:
  - "stable"
  - 4
  - 6

before_script:
  - npm install -g grunt-cli
  - npm install

script:
  - grunt
  - npm test
  # - npm run-script coverage

after_success:
  # - cat ./lib-cov/lcov.info |coveralls

cache:
 directories:
   - $HOME/.npm
   - node_modules
