language: node_js
sudo: false
node_js:
  - "0.10"
  - "4"
  - "6"

cache:
  directories:
  - node_modules

before_script:
  - npm install -g grunt-cli
  - npm install

script:
  - grunt
  - npm test
  # - npm run-script coverage

after_success:
  # - cat ./lib-cov/lcov.info |coveralls

matrix:
  fast_finish: true
